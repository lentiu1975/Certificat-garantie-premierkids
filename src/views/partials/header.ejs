<!DOCTYPE html>
<html lang="ro">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Certificate Garanție PremierKids</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/bootstrap-icons.css" rel="stylesheet">
    <script src="/js/bootstrap.bundle.min.js"></script>
    <style>
        :root {
            --primary-color: #2c5aa0;
            --sidebar-bg: #1a1a2e;
        }
        body {
            background-color: #f8f9fa;
            min-height: 100vh;
        }
        .sidebar {
            position: fixed;
            top: 0;
            left: 0;
            width: 260px;
            height: 100vh;
            background: var(--sidebar-bg);
            padding: 20px 0;
            overflow-y: auto;
            z-index: 1000;
        }
        .sidebar .logo {
            padding: 0 20px 20px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            margin-bottom: 20px;
        }
        .sidebar .logo h4 {
            color: white;
            margin: 0;
            font-weight: 600;
        }
        .sidebar .logo small {
            color: rgba(255,255,255,0.5);
            font-size: 12px;
        }
        .sidebar-nav {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .sidebar-nav li {
            margin: 2px 10px;
        }
        .sidebar-nav a {
            display: flex;
            align-items: center;
            padding: 12px 15px;
            color: rgba(255,255,255,0.7);
            text-decoration: none;
            border-radius: 8px;
            transition: all 0.2s;
        }
        .sidebar-nav a:hover, .sidebar-nav a.active {
            background: rgba(255,255,255,0.1);
            color: white;
        }
        .sidebar-nav a i {
            margin-right: 12px;
            font-size: 18px;
        }
        .sidebar-nav .nav-section {
            padding: 15px 20px 8px;
            color: rgba(255,255,255,0.4);
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .main-content {
            margin-left: 260px;
            padding: 30px;
            min-height: 100vh;
        }
        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }
        .top-bar h1 {
            font-size: 24px;
            font-weight: 600;
            margin: 0;
            color: #333;
        }
        .card {
            border: none;
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
        }
        .card-header {
            background: white;
            border-bottom: 1px solid #eee;
            font-weight: 600;
            padding: 15px 20px;
        }
        .btn-primary {
            background: var(--primary-color);
            border-color: var(--primary-color);
        }
        .btn-primary:hover {
            background: #234a8a;
            border-color: #234a8a;
        }
        .badge-new {
            background: #ff6b6b;
            color: white;
        }
        .table th {
            font-weight: 600;
            color: #666;
            border-bottom: 2px solid #eee;
        }
        .alert-floating {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 9999;
            min-width: 300px;
            animation: slideIn 0.3s ease;
        }
        @keyframes slideIn {
            from { transform: translateX(100%); opacity: 0; }
            to { transform: translateX(0); opacity: 1; }
        }
        .status-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            display: inline-block;
            margin-right: 6px;
        }
        .status-dot.active { background: #28a745; }
        .status-dot.inactive { background: #dc3545; }
        .loading-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(255,255,255,0.8);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
        }
        .user-menu {
            position: relative;
        }
        .user-menu .dropdown-menu {
            min-width: 200px;
        }

        /* Module Cards Styles */
        .module-launcher {
            max-width: 1000px;
            margin: 0 auto;
        }
        .module-card {
            display: flex;
            align-items: center;
            padding: 30px;
            background: white;
            border-radius: 16px;
            text-decoration: none;
            color: inherit;
            box-shadow: 0 4px 20px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            border-left: 5px solid;
        }
        .module-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0,0,0,0.15);
            color: inherit;
        }
        .module-icon {
            width: 70px;
            height: 70px;
            border-radius: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 32px;
            flex-shrink: 0;
        }
        .module-info {
            flex-grow: 1;
            margin-left: 25px;
        }
        .module-info h3 {
            margin: 0 0 8px 0;
            font-size: 22px;
            font-weight: 600;
        }
        .module-info p {
            margin: 0;
            color: #666;
            font-size: 14px;
        }
        .module-arrow {
            font-size: 24px;
            color: #ccc;
            transition: transform 0.3s;
        }
        .module-card:hover .module-arrow {
            transform: translateX(5px);
            color: #666;
        }

        /* Module Colors */
        .module-certificates {
            border-left-color: #2c5aa0;
        }
        .module-certificates .module-icon {
            background: rgba(44, 90, 160, 0.1);
            color: #2c5aa0;
        }
        .module-preturi {
            border-left-color: #28a745;
        }
        .module-preturi .module-icon {
            background: rgba(40, 167, 69, 0.1);
            color: #28a745;
        }
        .module-crawler {
            border-left-color: #6f42c1;
        }
        .module-crawler .module-icon {
            background: rgba(111, 66, 193, 0.1);
            color: #6f42c1;
        }
        .module-piese {
            border-left-color: #fd7e14;
        }
        .module-piese .module-icon {
            background: rgba(253, 126, 20, 0.1);
            color: #fd7e14;
        }
        .module-nomenclator {
            border-left-color: #17a2b8;
        }
        .module-nomenclator .module-icon {
            background: rgba(23, 162, 184, 0.1);
            color: #17a2b8;
        }

        /* Disabled module style */
        .module-card[onclick*="showComingSoon"] {
            opacity: 0.7;
        }
        .module-card[onclick*="showComingSoon"]:hover {
            transform: translateY(-2px);
        }

        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s;
            }
            .sidebar.show {
                transform: translateX(0);
            }
            .main-content {
                margin-left: 0;
            }
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <nav class="sidebar">
        <div class="logo">
            <h4>PremierKids</h4>
            <small>Management Tools</small>
        </div>

        <ul class="sidebar-nav">
            <li><a href="/" class="<%= title === 'Dashboard' ? 'active' : '' %>">
                <i class="bi bi-grid-1x2"></i> Dashboard
            </a></li>

            <li class="nav-section">Certificate Garanție</li>
            <li><a href="/certificates/auto" class="<%= title.includes('Automat') ? 'active' : '' %>">
                <i class="bi bi-play-circle"></i> Procesare Automată
            </a></li>
            <li><a href="/certificates/manual" class="<%= title.includes('Manual') ? 'active' : '' %>">
                <i class="bi bi-file-earmark-plus"></i> Generare Manuală
            </a></li>
            <li><a href="/history" class="<%= title === 'Istoric Certificate' ? 'active' : '' %>">
                <i class="bi bi-clock-history"></i> Istoric
            </a></li>

            <li class="nav-section">Prețuri</li>
            <li><a href="/prices" class="<%= title === 'Prețuri' ? 'active' : '' %>">
                <i class="bi bi-currency-exchange"></i> Management Prețuri
            </a></li>
            <% if (typeof user !== 'undefined' && user.isAdmin) { %>
            <li><a href="/prices/channels" class="<%= title === 'Canale de Vânzare' ? 'active' : '' %>">
                <i class="bi bi-shop"></i> Canale Vânzare
            </a></li>
            <% } %>

            <li class="nav-section">Crawler</li>
            <li><a href="#" onclick="showComingSoon('Crawler'); return false;" class="text-muted">
                <i class="bi bi-robot"></i> În curând
            </a></li>

            <li class="nav-section">Piese de schimb</li>
            <li><a href="#" onclick="showComingSoon('Piese de schimb'); return false;" class="text-muted">
                <i class="bi bi-gear"></i> În curând
            </a></li>

            <li class="nav-section">Global</li>
            <li><a href="/nomenclator" class="<%= title === 'Nomenclator Produse' ? 'active' : '' %>">
                <i class="bi bi-box-seam"></i> Nomenclator
                <% if (typeof newProductsCount !== 'undefined' && newProductsCount > 0) { %>
                    <span class="badge badge-new ms-auto"><%= newProductsCount %></span>
                <% } %>
            </a></li>

            <% if (typeof user !== 'undefined' && user.isAdmin) { %>
            <li class="nav-section">Administrare</li>
            <li><a href="/settings" class="<%= title === 'Setări' ? 'active' : '' %>">
                <i class="bi bi-sliders"></i> Setări
            </a></li>
            <% } %>
        </ul>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
        <div class="top-bar">
            <h1><%= title %></h1>
            <div class="user-menu dropdown">
                <button class="btn btn-outline-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown">
                    <i class="bi bi-person-circle"></i>
                    <%= typeof user !== 'undefined' ? user.username : 'Utilizator' %>
                </button>
                <ul class="dropdown-menu dropdown-menu-end">
                    <li><a class="dropdown-item" href="#" data-bs-toggle="modal" data-bs-target="#changePasswordModal">
                        <i class="bi bi-key"></i> Schimbă parola
                    </a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item text-danger" href="/auth/logout">
                        <i class="bi bi-box-arrow-right"></i> Deconectare
                    </a></li>
                </ul>
            </div>
        </div>
